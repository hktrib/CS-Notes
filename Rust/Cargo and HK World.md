![[Pasted image 20230227163712.png]]

### Functions
	* denoted by fn


### Build for Release
```cargo build --release ```
* Compiles and creates an executeble with optimizations
	* Makes code run faster
	* Longer time to Build executable
	* Good to benchmark code's running time
